#include <stdio.h>
#include <stdlib.h>

void getnumbers (int *a, int *b)
{
	printf("Enter the first number:");
	scanf("%d", &a);
	printf("Enter the second number:");
	scanf("%d", &b);
}
int sum (int a, int b)
{
return a+b;
}

